# SaaS Stripe Mock

A full-stack SaaS subscription system built with:

- Bun runtime
- Express (TypeScript)
- Firebase Authentication
- Firestore
- Stripe Subscriptions
- Vite + React frontend

This project demonstrates a complete subscription flow including authentication, protected routes, payment intent handling, and subscription lifecycle management.